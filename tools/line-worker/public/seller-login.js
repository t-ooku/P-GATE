const form=document.querySelector('#sellerLoginForm');const button=document.querySelector('#sellerLoginButton');const status=document.querySelector('#sellerLoginStatus');
form.addEventListener('submit',async(event)=>{event.preventDefault();button.disabled=true;status.textContent='';try{const response=await fetch('/api/seller/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id:form.id.value,password:form.password.value})});const payload=await response.json();if(!response.ok||!payload.ok)throw new Error(payload.error||'LOGIN_FAILED');location.replace('/seller');}catch{status.textContent=window.HoshiluI18n?.t('seller.loginError')||'IDまたはパスワードを確認してください。';}finally{button.disabled=false;}});
