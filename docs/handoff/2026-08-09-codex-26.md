# Codex → Claude 引き継ぎ報告（2026-08-09 23:40 JST）

## 対象コミット

- ローカル実装: `908b0f0`（通常検索へ高速AI検索語変換を統合）
- GitHub反映: `35cbcac671df02541df848864de0c419bee78b36`

## やったこと

- 通常の「ホシル検索」でも、ユーザーの曖昧な表現をGemini/OpenAIでブランド名・商品名・カテゴリを含む検索語へ変換してから、接続済みモールAPI・D1・13モール検索リンクへ渡すようにした。
- 変換は既存のGAS/D1検索と並列実行し、Gemini利用時は`gemini-3.1-flash-lite`、最小思考、最大128出力トークン、全体2.5秒上限にした。
- AIが元条件を省略しても、ユーザーの原文を必ず検索語へ残すマージ処理を追加した。
- AIが価格・URLを検索語へ混ぜても除去し、AIの出力自体を「実在商品」として表示しない境界を維持した。
- AI変換が失敗・タイムアウトした場合は、従来どおり原文で検索を続ける。
- `カラコン ローラ 度入り` → `LILMOON リルムーン 度あり カラコン ...`の変換を模擬した回帰テストを追加した。
- Service Workerを`v353`へ更新した。

## 意思決定・根拠

- 一件ごとの別名辞書だけでは未知の人物名・通称・視覚的手掛かりへ一般化できないため、確定した知識ルールは高速フォールバックとして残しつつ、通常検索の入口に汎用AI変換を追加した。
- AIの誤認リスクはゼロではない。そのためAIは検索仮説だけを作り、商品名・価格・在庫・URLの事実確認は既存のモールAPI/D1/許可済み遷移先に限定した。
- Gemini設定時はOpenAIへ同じ通常検索を二重送信せず、費用と遅延を抑えた。Gemini未設定時のみOpenAIを使用できる。
- Gemini公式ドキュメントでFlash-Liteが低遅延・高スループット向けであること、`thinkingLevel=minimal`・`maxOutputTokens`・JSON応答指定が利用可能であることを確認した。

## テスト・デプロイ結果

- `npm test`: 1295/1295 PASS
- `git diff --check`: PASS
- GitHub branch: `feature/ui-search-v2`へ反映成功
- デプロイ: 成功
- 本番確認: `service-worker.js` = `hoshilu-shell-v353`
- 本番確認: `/health` = `ok:true`、`ai_chat_configured:true`、楽天/Yahoo接続済み
- 本番の`/api/knowledge`実検索は有効なTurnstileトークンが必要なため、サーバー外からの自動実行は未実施。大隆さんの実機検索で最終確認が必要。

## Claudeに調査・レビューしてほしいこと

- コワークへの指示可否：可（木曜のクレジット復旧後）
- 差分`acc0325d..35cbcac`について、AI検索語変換が商品事実の公開境界を越えていないかレビューしてほしい。
- 人物名・俗称から誤ったブランドを推定した場合でも、元検索条件が残り、モールAPIの実商品以外を確定表示しないことを再確認してほしい。
- 実機で「カラコン ローラ 度入り」と未知の曖昧検索を試し、検索精度と体感速度を確認してほしい。
- 通常検索1回あたりの追加AI呼び出し費用と、本番p50/p95レイテンシの計測方法についてセカンドオピニオンがほしい。

## 次にやる予定

- 大隆さんの実機結果を受け、変換精度・誤変換・体感速度を調整する。
- 本番の検索トレースへ個人情報を残さず、AI変換の適用率・タイムアウト率・p50/p95を観測できる集計を検討する。
