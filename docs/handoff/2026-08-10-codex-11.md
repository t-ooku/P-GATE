# Codex → Claude 引き継ぎ報告（2026-08-10 05:20 JST）

## 対象コミット

- 本番反映: `f22eca2..84e1032`
- 対象PR: なし（本番ブランチ `feature/ui-search-v2` へ直接反映）

## やったこと

- HOSHILUのBETA表記を公開画面・規約・ランキングAPIから撤去し、正式運用表記へ移行。
- API連携表示を楽天市場・Yahoo!ショッピングの2モールに限定。Amazonは公式API接続まで他11モールと同じ個別検索導線へ移動。
- 通常検索と関連商品取得からAmazon Creators API呼び出しを停止。将来接続用の実装自体は削除せず保持。
- AI最安比較の実価格対象も楽天・Yahoo!の連携モール定義へ統一。
- 匿名KPIに `ai_result_clicked` / `ranking_result_clicked` / `price_comparison_opened` / `returning_visit` を追加。検索本文は保存しない。
- X・Instagramの14日ローリング自動販促を正式版キャンペーン `hoshilu-official-13mall-v2` へ更新。正式版、ランキング、AI最安比較、値下がり通知を訴求。
- Service Workerを `hoshilu-shell-v365`、`app.js`を `v116` へ更新。

## 意思決定・根拠

- Amazon API未接続の状態を「連携済み」と見せないため、表示・検索モード・AI最安比較の共通定義を `direct` に変更した。Amazonの検索リンクと13モール対象は維持する。
- Amazon API実装を削除すると接続後の再実装コストが増えるため、公開検索経路からだけ外した。
- NAVER型の「検索意図→商品発見→比較→送客」を評価できるよう、表示回数ではなく導線到達イベントを追加した。個人情報と検索本文はイベントへ含めない。
- 無料販促は既存の承認済み自動投稿基盤と重複防止（post_id/UPSERT）を再利用し、新キャンペーンIDで正式版の訴求へ切り替えた。

## テスト・デプロイ結果

- `npm ci --cache /tmp/hoshilu-npm-cache`: 成功
- `npm test`: **1342/1342 PASS**（fail 0）
- `node --check`: `app.js` / `growth-analytics.mjs` / `index.mjs` / `social-autopilot.mjs` 成功
- `git diff --check`: 成功
- デプロイ: 成功（本番アセット更新を確認）
- 本番確認: `/health?release=84e1032` は `ok:true` / `missing:[]` / `weak:[]`。楽天・Yahoo!はconfigured、Amazon Creators/SP-APIは未設定。公開HTMLはBETAなし・`app.js?v=116`・2連携/11個別表記、Service Workerは`hoshilu-shell-v365`。

## Claudeに調査・レビューしてほしいこと

**指示可否: 可（木曜のクレジット復旧後）**

1. 差分 `f22eca2..6f714ba` でAmazonが通常検索・関連商品・AI最安比較では未連携扱いになり、検索リンク自体は残っていること。
2. BETA表記の公開面からの撤去と、規約の免責内容が維持されていること。
3. 新KPI4種が検索本文を保存せず、既存growth_eventsと互換であること。
4. 自動販促が正式版キャンペーンへ更新され、誇大表現やAmazon API連携済み誤認がないこと。

## 次にやる予定

- GitHub Actionsのtest/deploy成功と本番反映を確認。
- 正式運用のKPIを蓄積し、AI/ランキング検索→商品クリック率、AI最安比較到達率、モール送客率、再訪率を週次で確認する。
- Amazon公式APIの契約・認証が整った段階で、明示的な接続確認後にのみAPI連携へ戻す。
