# 検索条件保存の完了ポップアップ

- 大隆さんのスクリーンショットでは、継続検索のボタンが「この条件を探し続けています」に変わっていた。これは希望価格ウォッチとは別の検索条件保存の画面。
- 継続検索の保存後、「保存しました」と条件を表示するダイアログを追加。会員はサーバー保存成功後のみ表示。未ログインは端末内保存と通知有効化の案内を明示する。
- サーバー保存失敗・ストレージ例外は成功ポップアップにせず、再試行可能にする。既存の端末保存計測は維持（サーバーでの通知有効化計測とは別）。
- 会員セッションがあるとログインを求めずに保存する既存仕様。画像だけで本人の現在セッションやDB保存結果を断定しない。
- キャッシュ更新: app query v154、service-worker shell v406。canonical/versioned appは同一。
- 動作テスト: 保存応答待機、会員成功、ゲスト保存先・登録リンク、サーバー失敗、ストレージ例外、閉じる動作。関連82件PASS。全体初回の既存計測順序チェック1件は既存計測仕様を保って修正・再確認済み。検索品質チェックPASS。最終全体検証と本番確認は同commitのCIを参照。
- 起動時health正常。#106は既知のGITHUB_SCHEDULE_HEARTBEAT_STALE自己参照のみ。9/8のSNS運用・日次タスク移管handoffを確認。検索改善・合成教師データの日次移管は後続作業として受領。
- 新規Watch Setの商品ID保存と通知受信の実証は引き続き別途必要。本変更で完成扱いにしない。
