# Codex → Claude 引き継ぎ報告（2026-08-09 20:05 JST）

## 対象コミット

- `8dbafc0`（「カラコン ローラ 度入り」をLILMOONへ補正）

## やったこと

- 実検索文「カラコン ローラ 度入り」を再現し、Query Expansionが未適用で入力がそのままモールへ渡っていたことを確認した。
- カラコン・ローラ・度入りの3条件が共起した場合だけ、`LILMOON リルムーン 度あり`を検索文先頭へ追加するルールを実装した。
- ローラ単独、または一般的な度入りカラコン検索をLILMOONへ誤変換しない否定テストを追加した。
- Amazon・楽天向け検索語にもLILMOONが残る回帰テストを追加した。
- Service Workerを`v352`へ更新。

## 意思決定・根拠

- Geminiは通常検索で候補が0件の時だけ商品解析を行う設計で、一般カラコン候補が出ると起動しない。設定不良ではなく起動条件と別名辞書の欠落が原因だった。
- 全検索でGeminiを先行実行すると速度と費用が悪化するため、今回確認された正解を再現可能なQuery Expansionへ追加した。
- LILMOON公式サイトで度数展開を確認した。現行イメージモデルは南部桃伽さんだが、ユーザーが提示した過去の「ローラ」手掛かりを検索別名として保持する。

## テスト・デプロイ結果

- `npm test`: 1293/1293 PASS
- `git diff --check`: PASS
- デプロイ・本番確認: push後に実施予定

## Claudeに調査・レビューしてほしいこと

- コワークへの指示可否：可（木曜のクレジット復旧後）
- `lilmoon-rola-colored-contacts`ルールが他のローラ商品へ誤発火しないかレビューしてほしい。
- 実機で「カラコン ローラ 度入り」を検索し、LILMOON商品またはLILMOON検索導線が最初から出ることを確認してほしい。

## 次にやる予定

- 本番反映後にService Worker `v352`と`/health`を確認。
- 同種の「人物名→商品ブランド」失敗をTeacher Datasetへ蓄積する運用を継続する。
