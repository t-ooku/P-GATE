# Codex → Claude 引き継ぎ報告（2026-08-10 19:06 JST）

## 対象コミット

- 本番反映: `3749339..4e2b2b4`
- 対象PR: なし（本番ブランチ `feature/ui-search-v2` へ直接反映）

## やったこと

- Instagramリールの定期投稿を週2回から週3回へ変更。毎週月・火・土の20:15 JSTに設定した。
- 本日2026-08-10（月）の20:15枠も14日ローリング自動補充の対象にした。
- 管理者専用の販促運用ダッシュボード `/admin/promotion` を追加した。
- X・Instagram・TikTokを別カードで一覧化し、接続状態、通常スケジュール、次回予約、予約・公開・失敗件数、直近5件の結果を表示する。
- 既存の管理者セッション、30分再認証、no-store、CSPを再利用し、一般公開APIにはしなかった。
- Service Workerを `hoshilu-shell-v366` へ更新した。

## 意思決定・根拠

- 月曜追加分も静止画ではなく既存の権利確認済み縦型MP4を利用し、Instagramのリール重視方針を維持した。
- 月・火・土で本文と動画の割当を変え、同一内容の連投を避けた。
- 投稿計画ではなくD1の実キューをダッシュボードの正としたため、公開失敗や予約状態も同じ画面で確認できる。
- 販促情報には投稿本文・失敗理由が含まれるため、既存の管理者認証内だけで表示した。

## テスト・デプロイ結果

- `npm test`: **1346/1346 PASS**（fail 0）
- 対象テスト: Instagram月・火・土20:15、チャネル別集計、未認証API拒否、管理画面Secret非露出
- `node --check`: `promotion-dashboard.mjs` / `admin-promotion.js` 成功
- `git diff --check`: 成功
- デプロイ: 成功
- 本番確認: Service Worker `hoshilu-shell-v366`、`/health` は `ok:true`、`social_autopilot_enabled:true`、X/Instagram publisherはtrue、TikTokはfalse
- 未ログインで `/admin/promotion` が `/admin-login` へ302になることを確認
- D1キューの本日20:15行は管理Secretをこの環境へ展開していないため直接閲覧していない。デプロイ後15分以内のcronで自動補充される設計と回帰テストは確認済み。

## Claudeに調査・レビューしてほしいこと

**指示可否: 可（木曜のクレジット復旧後）**

1. 差分 `3749339..4e2b2b4` の管理認証・CSP・D1集計に情報漏えいがないこと。
2. 月・火・土20:15の曜日計算、14日ローリング補充、既存行の冪等性を再確認すること。
3. 実管理ログイン後、スマホ幅とPC幅で3チャネルの一覧が見切れないこと。
4. 2026-08-10 20:15 JST後、Instagramリールの実投稿URLと公開時刻を確認すること。

## 次にやる予定

- 3万円プラン期間中に、検索品質の自動評価、購買導線KPI、障害検知、スポンサー販売に必要な証跡を優先して強化する。
- ダッシュボードへ投稿成果スナップショットが蓄積された後、媒体別クリック・検索開始・モール送客を同じ画面へ追加する。
