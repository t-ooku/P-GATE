# HOSHILU Brand Guide v1.0

## Brand

- Japanese display name: `ホシル`
- Latin display name: `HOSHILU`
- Reading: ホシル
- Brand line: `欲しいを、ちゃんと見つける。`
- Habit line: `気になったら、ほしっとく。`
- Conversational prompt: `これ、ホシっといて。`

`HOSHILU` is the public brand. `Project GATE` remains the internal project name until
repository, contracts, infrastructure, and integrations are deliberately migrated.

## Product language

| Existing/internal name | Public label | Meaning |
|---|---|---|
| MYCONCIERGE | ホシル | Describe an unclear need and find candidates together |
| MYWISH | ほしっとく | Save a want on the device and continue later |
| MYWATCH | ホシっといて | Ask HOSHILU to watch availability, price, coupon, and restock changes |
| MYTREASURE | HOSHILU INSIGHT | Seller-side aggregated demand and opportunity analysis |

Internal API fields, storage keys, sheet names, and historical metrics keep their current
identifiers during the compatibility phase. Public copy must not expose the old MYGATE name.

## Portrait palette

The existing portrait palette is retained.

| Token | Value | Use |
|---|---|---|
| Pink | `#F238B5` | Warm desire, social accents |
| Violet | `#8A4CF4` | Primary brand |
| Blue | `#258CFF` | Discovery, action |
| Ink | `#111C4E` | Text |
| Mist | `#F7F5FF` | Background |
| White | `#FFFFFF` | Canvas |

Primary gradient: `linear-gradient(135deg, #F238B5 0%, #8A4CF4 48%, #258CFF 100%)`.

## Mark

The HOSHILU mark combines:

- a rounded `H` for HOSHILU;
- a heart-like center bridge for “欲しい”;
- a small sparkle for discovery;
- the inherited pink-violet-blue gradient.

Keep clear space equal to one quarter of the mark width. Do not recolor the gradient,
stretch the mark, add third-party marketplace logos, or place detailed photography behind it.

## Voice

- Short, conversational, and encouraging.
- Say `ホシる`, `ほしっとく`, and `ホシっといて` where the action is clear.
- Avoid claiming that a product is definitely correct, safe, compatible, cheapest, or in stock.
- Use `販売ページで確認` before purchase.

## Social handles

Preferred handles, in order:

1. `@hoshilu`
2. `@hoshilu_jp`
3. `@hoshilu_official`
4. `@hoshilu_app`

Handle availability must be checked inside each platform immediately before registration.
Do not retain `mygate` in a new public handle.

## Hashtags

Primary: `#ホシル`

Campaign and interaction:

- `#ほしっとく`
- `#ホシった`
- `#今日のホシル`
- `#これホシっといて`
